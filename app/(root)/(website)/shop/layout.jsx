export const metadata = {
    title: 'Shop All Shoes | Trendy Steps',
    description: 'Explore running, training, lifestyle, outdoor, and court collections. Filter by size, brand, and price — VAT-included BDT pricing.',
    alternates: { canonical: '/shop' }
}

export default function ShopLayout({ children }) {
    return children
}
