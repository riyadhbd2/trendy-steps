export const metadata = {
    title: 'Your Cart | Trendy Steps',
    description: 'Review the shoes in your cart, adjust quantities, and proceed to checkout.',
    robots: { index: false, follow: false },
    alternates: { canonical: '/cart' }
}

export default function CartLayout({ children }) {
    return children
}
